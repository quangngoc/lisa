# Tells Git to automatically detect whether files are text or binary
* text=auto

# Explicitly declare text files to always be normalized and converted to native line endings on checkout.
*.py text
*.md text
*.ini text
*.yaml text
*.yml text
*.toml text

# These files will not be normalized
*.whl binary